/**
 * @file
 * drupalcustomcommerce behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.drupalcustomcommerce = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
